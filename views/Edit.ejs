<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-blue-50 min-h-screen flex items-center justify-center text-gray-900">
    <div class="bg-white shadow-lg rounded-xl p-8 w-3/4 md:w-2/3 lg:w-1/3 border-l-4 border-blue-500">
        <h1 class="text-3xl font-bold text-center text-blue-700 mb-6">📝 Edit Post</h1>

        <form action="/posts/<%= post.id %>?_method=PATCH" method="post" class="flex flex-col space-y-4">
            <input type="text" name="username" id="username" class="p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" value="<%= post.username %>" readonly>
            
            <textarea name="content" id="content" class="p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"><%= post.content %></textarea>
            
            <button type="submit" class="bg-blue-600 text-white px-6 py-3 rounded-lg font-semibold hover:bg-blue-700 transition" onclick="return alert('Post updated successfully!')">Update</button>
        </form>
    </div>
</body>
</html>
